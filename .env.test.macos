# macOS-specific test configuration
# Uses memory SQLite to avoid file locking issues
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
DATABASE_URL=sqlite:///:memory:

# Disable DAMA bundle features that cause segmentation faults on macOS
DAMA_DOCTRINE_TEST_BUNDLE_USE_STATIC_CONNECTION=true
DAMA_DOCTRINE_TEST_BUNDLE_ENABLETransactional=false

# PHP configuration for macOS stability
memory_limit=512M